Test data (fixtures, sample files for upload tests, JSON/CSV datasets) goes here.
Reference the old project's `assets/` folder for the kind of files historically
needed (sample xlsx/pdf/csv for trial balance and document upload tests) —
re-source or regenerate them here as needed per feature.
